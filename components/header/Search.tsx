import React from 'react';
import Button from '../UI/Button';

const Search = () => {
  return (
    <form>
      <input placeholder='search for something more specific'></input>
      <Button></Button>
    </form>
  );
};

export default Search;
